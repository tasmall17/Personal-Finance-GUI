# Nobody's finances belong in this repository.
REVIEW.md
profiles/*.json
!profiles/demo.json
profiles/.active
data/
review/
serve.log
.venv/
__pycache__/
*.pyc
node_modules/
# macOS sprinkles these through every folder you have ever opened in Finder.
.DS_Store
# Credentials belong in ~/.config/flightdeck/, never in the working copy.
*.env
# The demo dataset ships, so a clone works without running the generator.
# It is regenerated by demo/generate_demo.py if you want to change it.
